# ===========================================
# BLACKROCK Environment Configuration
# ===========================================
# Copy this file to .env.local for development
# NEVER commit .env.local or .env.production to git
# Use proper secrets management for production
# ===========================================

# App URL (required)
NEXTAUTH_URL=http://localhost:3000

# NextAuth Secret (required)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
NEXTAUTH_SECRET=your_nextauth_secret_here_generate_new_one

# MongoDB Connection (required)
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>
MONGODB_URI=mongodb://localhost:27017/blackrock

# Field Encryption Key (required for sensitive data)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
FIELD_ENCRYPTION_KEY=your_encryption_key_here_generate_new_one

# Google OAuth (optional - for Google Sign In)
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

# Email Configuration (required for email verification)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SENDER_EMAIL=your_email@gmail.com
SENDER_PASSWORD=your_app_password_here

# Cloudinary (required for file uploads)
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# Redis (optional - for rate limiting in production)
# REDIS_URL=redis://localhost:6379

# Cron Job Secret (required for scheduled tasks)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
CRON_SECRET=your_cron_secret_here_generate_new_one

# Deposit Wallet Address (required)
DEPOSIT_WALLET_ADDRESS=your_wallet_address_here

# ===========================================
# SECURITY NOTES
# ===========================================
# 1. NEVER commit real credentials to version control
# 2. Use different credentials for development and production
# 3. Rotate secrets regularly (at least quarterly)
# 4. Use a secrets manager in production (e.g., Vercel Secrets, AWS Secrets Manager)
# 5. Limit access to production secrets
# ===========================================
